import { ArticleListConfig } from './article-list-config';

describe('ArticleListConfig', () => {
  it('should create an instance', () => {
    expect(new ArticleListConfig()).toBeTruthy();
  });
});
